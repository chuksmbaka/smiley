package com.company;
/*
public class AnonymousClass {

    //creates the anonymous class. we define the interface methods within the anonymous class
    Checkable checkObject = new Checkable() {
        @Override
        public boolean check() {
            return false;
        }
    }
}

 */
